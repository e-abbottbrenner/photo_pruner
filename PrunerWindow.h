#ifndef PRUNERWINDOW_H
#define PRUNERWINDOW_H

#include <QMainWindow>

class PrunerWindow : public QMainWindow
{
    Q_OBJECT
public:
    explicit PrunerWindow(QWidget *parent = 0);
    ~PrunerWindow();

signals:

public slots:
};

#endif // PRUNERWINDOW_H
